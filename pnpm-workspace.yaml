# Spararama's main app is the dependency-bearing package. Hardware adapter
# services are kept under services/ and may be deployed independently; the
# CleverSpa service currently has no third-party runtime dependencies.
allowBuilds:
  '@firebase/util': true
  '@google/genai': true
  esbuild: true
  protobufjs: true
