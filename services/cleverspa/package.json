{
  "name": "@spararama/cleverspa-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Standalone CleverSpa/Gizwits adapter service for Spararama",
  "engines": { "node": ">=22.9.0" },
  "scripts": {
    "dev": "node --env-file-if-exists=.env --watch src/server.js",
    "start": "node --env-file-if-exists=.env src/server.js",
    "test": "node --test"
  }
}
